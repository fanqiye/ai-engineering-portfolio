name: PowerShell checks
on: [push, pull_request, workflow_dispatch]
permissions:
  contents: read
jobs:
  check:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run self-check
        shell: powershell
        run: .\tests\test-serial-recorder.ps1
