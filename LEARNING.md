# 项目理解与独立复现

这两个项目由 Codex 辅助实现并运行。先把流程讲清楚，再把实际独立完成的
改进写入简历。当前可以如实说“有 AI 辅助开发的个人项目”，不能说自己已经
独立掌握模型训练、做过公司算法研发或取得线上业务收益。

## 先跑通

按 README 建立虚拟环境并安装 NumPy。运行 `python check.py`，应出现
`CHECK_OK`。首次分类实验使用 `python spam_classifier.py --download`，检索
使用 `python retrieval_eval.py`。用 `--out .cache/my-results` 保存自己的复现
结果，避免覆盖参考结果。比较 `spam_metrics.json` 中的混淆矩阵和 F1；耗时
会受电脑和环境影响。运行不需要付费模型 API。

分类真实调用链是：读取并校验 ZIP → 解析五个 CSV → 标准化去重 → 按视频
划分 → 训练集建立词表 → 计数或 TF-IDF → 训练 → 验证集选阈值 → 测试评估。
检索真实调用链是：加载知识库和题目 → 中文二元字串分词 → 词频与文档频率
统计 → overlap 或 BM25 排序 → 开发集选阈值 → 测试并保存错误案例。

## 能解释这些问题再用于技术面试

1. 为什么按视频划分？随机划分可能让重复评论和同一视频风格跨越训练与测试。
   当前先做精确去重；近重复和语义重复仍可能存在。
2. 为什么 IDF 只在训练集拟合？让测试文档参与词表或频率估计，会改变训练时
   可获得的信息。IDF 为 `log((1+n)/(1+df))+1`，之后按行做 L2 归一化。
3. 逻辑回归优化什么？交叉熵加 L2 正则项。权重梯度为
   `X.T @ (sigmoid(Xw+b)-y)/n + lambda*w`，偏置梯度为误差均值。
4. 为什么有数值梯度检查？用很小的正负扰动近似导数，和解析梯度比较；它能
   发现训练过程“能跑但算错”的问题，不能证明整个模型适合现实数据。
5. 为什么不能只报 accuracy？多数类模型 accuracy 55.3%，却漏掉所有垃圾
   评论。逻辑回归也漏掉 29 条，必须一起看 precision、recall 和混淆矩阵。
6. 为什么逻辑回归没有赢？测试 F1 0.880，略低于朴素贝叶斯 0.884。验证集选
   的阈值不一定最适合另一视频。不能看测试答案调阈值后再称为独立测试。
7. AP 和阈值 F1 有何不同？AP 衡量不同分数阈值下的排序表现，F1 衡量一个
   指定阈值下的分类结果。高 AP 不保证当前决策阈值上的 F1 更高。
8. BM25 为什么比词语重合复杂？加入词语稀有度、词频饱和和文档长度修正；
   中文 bigram 仍然依赖字面匹配，不理解同义表达。
9. 95% Recall@3 意味着什么？40 道可回答的合成题中，38 道在前三条找到指定
   资料。它不等于回答正确率，也不等于真实用户问题上的成功率。
10. 为什么有来源也可能错？BM25 会为无答案问题返回高分但无关的内容。
    测试集 10 个无答案问题只拒绝了 6 个。引用来源不能替代相关性判断。

## 留下自己的下一次贡献

先独立修改一个输入校验或命令行功能，写清问题、改动、复现命令和结果，
让提交记录反映实际完成时间。不要补造旧提交或历史工作周期。

之后单独收集新问题并人工标注，至少包含同义改写、数字条件和无答案题。
先冻结新测试集，再比较方法；记录输给简单基线的情况。不要只把本次失败题
加入词表后在同一套题上宣布泛化提升。

分类方向可以规划跨视频的轮换评估，检索方向可以规划向量检索对照。这里
尚未实现这些扩展；只有亲自完成并留有结果后才能改写为已完成的简历条目。

能够不看答案解释上述流程、独立修复一个错误、复现实验并提出有依据的
下一步，才开始把项目文件转化为自己的能力。
